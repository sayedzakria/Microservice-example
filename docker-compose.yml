services:
  catalogdb:
    image: postgres
  basketdb:
    image: postgres
  distributedcashe:
    image: redis
  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: ./Services/Catalog/
      dockerfile: Catalog.API/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:

